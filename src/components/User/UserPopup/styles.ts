import { makeStyles, Theme } from "@material-ui/core";

export const useStyles = makeStyles((theme: Theme) => ({
  field: {
    
  },
  outerField: {
    margin: 10
  },
  dialog: {
    padding: 20
  }
}))